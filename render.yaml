# Render Blueprint — Ranking (Amaro/Intranet) · Jogos do Brasil (estático, sem build)
# Render ▸ New ▸ Blueprint ▸ conectar este repositório ▸ Apply.
services:
  - type: web
    name: bolao-balera-amaro-brasil
    runtime: static
    buildCommand: "echo site estatico sem build"
    staticPublishPath: .
